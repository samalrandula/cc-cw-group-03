# Ingress

This directory contains Kubernetes Ingress resources for external access.

## Files

1. `ingress.yaml` - Main ingress controller routing configuration

## What is Ingress?

Ingress:
- Exposes HTTP/HTTPS routes from outside the cluster
- Provides load balancing
- Routes traffic to internal services
- Gives you a single external IP for the entire application