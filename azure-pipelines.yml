trigger:
  branches:
    include:
      - main

pool:
  vmImage: 'ubuntu-latest'

steps:
  - checkout: self

  - script: |
      echo "CI pipeline started successfully"
      echo "Repo structure:"
      ls -R
    displayName: "Verify repo is loaded"