spring.datasource.url=jdbc:h2:mem:identity_service_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=SPRING_DATASOURCE_PASSWORD

# Keep tests lightweight: don't require running migrations
spring.liquibase.enabled=false
spring.jpa.hibernate.ddl-auto=none

# JWT test configuration
security.jwt.secret=changemechangemechangemechangemechangemechangemechangemechangeme
security.jwt.expiration-ms=3600000
